# دليل الفرونت-إند: الفرق بين `customer_personal` و `customer_company`

> **موجّه لوكيل Claude Code في مشروع الفلاتر.**
> هذا الملف يشرح كل فرق يجب أن تبنيه في الواجهة بين نوعَي العميل.
> التطبيق يخدم هذين الدورين فقط (لا أدمن ولا سوبر أدمن).
>
> **حالة التنفيذ:** شاشتا **التسجيل (register) للنوعين مكتملتان** — موثّقتان هنا كمرجع فقط.
> **الباقي (ملف الشركة، حجز الأسطول، باقات الشركات، العقود، التقارير) لم يُبنَ بعد** — هذا ما تركّز عليه.

---

## 0) القاعدة الذهبية

عميل الشركة يملك **كل** صلاحيات العميل الفردي **+ 5 صلاحيات إضافية**:

```
customer_company = customer_personal
                 + show.companies   // رؤية كيان الشركة
                 + edit.company     // تعديل بيانات شركته
                 + manage.fleet_cars// إدارة أسطول (حجز عدة سيارات)
                 + show.contracts   // العقود
                 + show.reports     // التقارير
```

**الخلاصة:** كل ما يفعله الفردي يفعله الشركة. الشركة = الفردي + طبقة إضافية.
لذا: ابنِ التجربة الأساسية مرة واحدة للاثنين، ثم أضِف أقسام الشركة **مشروطة بالدور**.

### كيف تعرف نوع المستخدم في الفلاتر
من ردّ تسجيل الدخول/التسجيل، الحقل `data.role`:
```dart
final isCompany = user.role == 'customer_company';
// 'customer_personal'  أو  'customer_company'
```
اجعل `isCompany` مصدر الحقيقة الوحيد لإظهار/إخفاء كل أقسام الشركة أدناه.

---

## 1) التسجيل (مكتمل — مرجع فقط)

فرق جوهري في السلوك يجب أن تعرفه حتى لو الشاشات جاهزة:

| | العميل الفردي | عميل الشركة |
|---|---|---|
| المسار | `POST /api/auth/register/customer` | `POST /api/auth/register/company` |
| التفعيل | **فوري** | **معلّق بموافقة السوبر أدمن** |
| التوكن في الرد | ✅ يرجع `token` → دخول مباشر | ❌ **لا token** |
| `is_active` بعد التسجيل | `true` | `false` |
| رسالة الرد | "Account registered successfully" | "Registration request submitted and is pending approval" |

**حقول الفردي:** `name`, `email`, `phone`, `password`, `password_confirmation`, `image_url?`.
**حقول الشركة:** نفس السابق + `company_name`, `company_name_ar`, `commercial_reg` (فريد), `tax_number` (فريد), `company_address`.

**سلوك الواجهة المطلوب:**
- الفردي: بعد النجاح خزّن `token` وادخل التطبيق مباشرة.
- الشركة: بعد النجاح اعرض شاشة **"طلبك قيد المراجعة"** ولا تحاول الدخول (لا يوجد توكن).
- ⚠️ لو حاول عميل شركة **غير مُفعّل** تسجيل الدخول لاحقاً، معظم المسارات تحجبه بـ **403 `"Your account is inactive."`** — عالجها بعرض حالة "قيد المراجعة" بدل خطأ عام.

---

## 2) كيان الشركة (Company) — **حصري لعميل الشركة** ⬅️ يحتاج بناء

العميل الفردي **لا شركة له إطلاقاً**. عميل الشركة له **سجل شركة واحد** مرتبط به.

### حقول كائن الشركة (شكل الرد)
```json
{
  "id": 3,
  "name": "Al-Salam Transport",
  "name_ar": "السلام للنقل",
  "commercial_reg": "1010101010",
  "tax_number": "300000000000003",
  "address": "الرياض - حي العليا",
  "status": "approved",         // pending | approved | rejected
  "is_active": true,
  "owner": { ... } | null,      // whenLoaded — عامله nullable
  "created_at": "2026-08-01"
}
```

### المسارات (كلها محميّة بـ Bearer token)
| العملية | المسار | ملاحظات للفرونت |
|---|---|---|
| **شركتي** | `GET /api/companies/my` | ⭐ الأساس — بلا معرّف، يجلب شركة المستخدم الحالي. لو لا شركة ⇒ 404 |
| عرض شركة | `GET /api/companies/{id}` | يتحقق أنها لك، وإلا **403 "Unauthorized"** |
| **تعديل شركتي** | `POST /api/companies/{id}` | تعديل (POST لا PUT). **لا ترسل `status` ولا `is_active`** — يتجاهلهما الباك (سوبر أدمن فقط يغيّرهما) |
| قائمة كل الشركات | `GET /api/companies` | صلاحية `show.companies` موجودة، لكنه عملياً للأدمن — **لا تستعمله في تطبيق العميل** |
| حذف | `DELETE /api/companies/{id}` | **سوبر أدمن فقط** — يرجع لك 403، لا تربطه |

### ما يجب بناؤه
- شاشة **"ملف الشركة"** تستدعي `GET /companies/my` وتعرض الحقول أعلاه.
- زر **تعديل** يستدعي `POST /companies/{id}` بالحقول القابلة للتعديل فقط (الاسم، العربي، العنوان، السجل، الرقم الضريبي) — **دون** `status`/`is_active`.
- اعرض `status` كشارة (badge): `pending` (قيد المراجعة) / `approved` (معتمد) / `rejected` (مرفوض).
- **لا تُظهر هذا القسم إطلاقاً للعميل الفردي.**

---

## 3) الحجز — الفردي سيارة واحدة، الشركة أسطول ⬅️ فرق سلوكي يحتاج بناء

هذا **ليس مجرد صلاحية** بل اختلاف في منطق الحجز نفسه (يفرضه الباك):

| | العميل الفردي | عميل الشركة |
|---|---|---|
| `car_ids` في الحجز | **سيارة واحدة فقط** | **عدة سيارات** |
| لو أرسل أكثر من سيارة | ❌ 422 `"Personal customers may only book one car at a time."` | ✅ يُنشئ طلباً لكل سيارة |
| ربط الطلبات | — | كلها بنفس `booking_group_id` و`company_id` |

> تذكير بتدفّق الحجز (مشترك للاثنين): خطوتان — `POST /api/bookings/quote` (تسعير + `quote_token` صالح 15 دقيقة) ثم `POST /api/bookings/confirm` بـ `{ "quote_token": "..." }`.

### ما يجب بناؤه (الفرق فقط)
- **واجهة الفردي:** اختيار سيارة **واحدة** (radio / dropdown). `car_ids` = مصفوفة بعنصر واحد.
- **واجهة الشركة:** اختيار **متعدد** للسيارات (checkboxes / multi-select) — "احجز لعدة سيارات دفعة واحدة".
- الفاتورة من `quote` تُرجع `car_count` و`total_price_per_car` و`total_price` (= per_car × العدد) — اعرض العدد والإجمالي بوضوح لعميل الشركة.
- بقية حقول الحجز وتدفّقه متطابقان تماماً.

```dart
// عند بناء طلب الحجز
final carIds = isCompany ? selectedCarIds : [singleSelectedCarId];
```

---

## 4) الباقات — نوع مختلف لكل دور ⬅️ فلترة تحتاج بناء

- كل الباقات ترجع من `GET /api/packages` (بلا فلترة من الباك).
- كل باقة لها `is_company_package` (bool) و`type` (`weekly`/`monthly`/`company`).

| | يشتري باقات |
|---|---|
| الفردي | `is_company_package == false` (weekly / monthly) |
| الشركة | `is_company_package == true` (باقات الأسطول، غالباً بخصم كمية) |

### ما يجب بناؤه
```dart
final visiblePackages = packages.where(
  (p) => p.isCompanyPackage == isCompany,
).toList();
```
اعرض لكل دور باقاته فقط. شراء الباقة عبر `POST /api/user-packages` (مشترك)، والدفع بالباقة في الحجز عبر `payment_method: "package"`.

---

## 5) العقود والتقارير — حصرية للشركة لكن ⚠️ **بلا API بعد** (لا تبنِها الآن)

**حقيقة مهمة جداً من الباك-إند:** الصلاحيتان `show.contracts` و`show.reports` **معرّفتان** لعميل الشركة،
لكن **لا يوجد أي endpoint فعلي لهما** — لا route، لا controller، لا resource. الموجود فقط
جداول قاعدة بيانات (`contracts`, `employee_reports`) وموديلات. أي: **المسارات غير جاهزة للربط.**

### ما العمل الآن
- ❌ **لا تبنِ شاشات العقود/التقارير الآن** — لا يوجد ما تربطه بعد (أي استدعاء سيرجع 404).
- ✅ يمكنك تجهيز **مكان القسم** في تطبيق الشركة (زر/تبويب مخفي أو "قريباً") مشروطاً بـ `isCompany`.
- انتظر توثيق المسارات لاحقاً عند بنائها في الباك.

### شكل البيانات المتوقّع (من الجداول — للتخطيط فقط، وليس عقداً نهائياً)
**Contract (عقد الشركة مع ورشة):** `id`, `company_id`, `workshop_id`, `created_by`, `title`,
`value` (decimal), `start_date`, `end_date`, `terms`, `file_url?`, `status`
(`draft`/`active`/`expired`/`terminated`/`renewed`), `renewal_count`, `last_renewed_at?`.

**"التقارير" (`employee_reports`):** ⚠️ ملاحظة دقيقة — هذا الجدول حالياً هو **تقارير الموظفين عن الطلبات**
(فنّي يصف مشكلة/قطع متضررة/صور/توصية لطلب معيّن)، **وليس** تقارير أسطول موجّهة للعميل. فلا تفترض
أنه مصدر "تقارير الشركة" حتى يتأكد ذلك عند بناء المسار.

> باختصار: القسم 5 **مؤجّل** — الصلاحية موجودة، لكن لا API. حدّث هذا الجزء عند وصول المسارات.

---

## 6) ما هو **مشترك تماماً** (لا فرق — نفس الشاشات والمسارات)

ابنِها مرة واحدة للنوعين:
- **السيارات:** إضافة/عرض/تعديل/حذف (`/api/cars/...`).
- **الحجوزات:** quote / confirm / قائمة / تفاصيل / إلغاء (الفرق الوحيد = عدد السيارات، القسم 3).
- **الكتالوج:** categories, services, sub-services, car-types, car-brands, branches.
- **النقاط والمحفظة:** `/api/points/...`, wallet.
- **باقاتي المشتراة:** `/api/user-packages/...`.
- **البروفايل:** `GET /api/profile/showProfile`, `POST /api/profile/updateProfile`.
- **الدفع، التقييم، الإشعارات.**

---

## 7) مصفوفة القرار السريعة للواجهة

| الميزة / القسم | الفردي | الشركة | ملاحظة التنفيذ |
|---|:---:|:---:|---|
| تسجيل | ✅ (فوري + توكن) | ✅ (معلّق، بلا توكن) | **مكتمل** |
| السيارات (CRUD) | ✅ | ✅ | مشترك |
| حجز سيارة واحدة | ✅ | ✅ | مشترك |
| حجز أسطول (عدة سيارات) | ❌ | ✅ | multi-select للشركة |
| ملف الشركة | ❌ | ✅ | `/companies/my` + تعديل |
| باقات الأفراد | ✅ | ❌ | فلترة `is_company_package` |
| باقات الشركات | ❌ | ✅ | فلترة `is_company_package` |
| العقود | ❌ | ✅* | *حصري للشركة لكن **بلا API بعد** — لا تبنِه الآن |
| التقارير | ❌ | ✅* | *حصري للشركة لكن **بلا API بعد** — لا تبنِه الآن |
| الكتالوج/النقاط/المحفظة/البروفايل | ✅ | ✅ | مشترك |

---

## 8) قواعد ذهبية عند التنفيذ

1. **`isCompany` مصدر الحقيقة الوحيد** لإظهار/إخفاء أقسام الشركة — خذه من `user.role`.
2. **لا تفترض فلترة من الباك:** قوائم `/packages` (وغيرها) ترجع الكل — فلتر محلياً حسب الدور.
3. **الشركة تُلحق تلقائياً بالطلبات** (`company_id`) في الباك — لا ترسله أنت.
4. **عند تعديل الشركة لا ترسل** `status`/`is_active` (تُتجاهَل).
5. **حالة الحساب غير النشط** (شركة قيد المراجعة): عالج 403 `"Your account is inactive."` كحالة "قيد المراجعة".
6. **الأخطاء موحّدة:** 401 (توكن)، 403 (صلاحية/ملكية)، 404 (غير موجود)، 422 (تحقّق مدخلات مع `data` = خريطة حقول).
7. **الغلاف الموحّد لكل رد:** `{ status, data, message, status_code, timestamp }`.
