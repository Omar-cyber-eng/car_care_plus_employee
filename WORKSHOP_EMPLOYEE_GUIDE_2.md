# دليل الورشة/الموظف — الجزء 2 (مستجدّات ما بعد التحديث)

> ملحق لـ `WORKSHOP_EMPLOYEE_GUIDE.md`. يغطّي **ما استجدّ** بعد سحب التحديثات:
> ثلاث ميزات كانت "بلا مسارات" (القسم 6 في الدليل الأول) **أصبح لها API الآن**،
> + توضيح **حالات الدفع كاملة**، + إجابة سؤال **ترتيب حالات الطلب مقابل الدفع**.

---

# 1) 🆕 ميزات صار لها مسارات (كانت مؤجّلة)

`commit: add spar part crud` أضاف: **قطع الغيار** + **تقارير الموظف** + **سجلّات GPS**.
(كما حُذف كيان **العقود Contracts** من الباك — لم يعد موجوداً، فتجاهله نهائياً.)

## 1.1 قطع الغيار — `spare-part-requests` (⚠️ إعادة تصميم مهمة)

**التدفّق الجديد:** الميكانيكي **يطلب** قطعة غيار على طلب مفتوح → **العميل (صاحب الطلب) يعتمد/يرفض** (وليس الورشة!).

| العملية | المسار | مَن (في تطبيقك) |
|---|---|---|
| إنشاء طلب قطعة | `POST /api/spare-part-requests` | **الميكانيكي فقط** (`create.spare_part_request`) |
| قائمة الطلبات | `GET /api/spare-part-requests` | **الميكانيكي فقط** (`show.spare_part_requests`) |
| تفاصيل طلب | `GET /api/spare-part-requests/{id}` | الميكانيكي فقط |
| اعتماد / رفض | `POST .../{id}/approve` \| `/reject` | ⛔ **العميل فقط** — خارج تطبيقك (لوحة العميل) |

- ⚠️ **الغسّال والورشة لا يملكان أي صلاحية قطع غيار** — لا إنشاء ولا عرض ولا اعتماد.
- **قاعدة عمل:** لا يمكن إنشاء طلب قطعة إلا والطلب **مفتوح** (ليس `completed` ولا `cancelled`) — وإلا خطأ.
- لا يمكن اعتماد/رفض طلب **سبق البتّ فيه** (`SparePartAlreadyDecided`).

**حقول الإنشاء (POST):** `order_id` (مطلوب)، `material_id` (مطلوب — من كتالوج `/materials`)،
`quantity` (≥1)، `specifications?`، `notes?`.
> ⚠️ لاحظ: القطعة تُختار من **`material_id`** (كتالوج المواد) وليست نصاً حرّاً.

**شكل الرد (SparePartRequestResource):**
```json
{ "id": 1, "order_id": 40, "order": {...}|null,
  "employee_id": 2, "employee": {...}|null,
  "material_id": 5, "material": {...}|null,
  "quantity": 2, "specifications": "...",
  "status": "pending",              // pending|approved|rejected|ordered|received
  "notes": "...", "decided_at": null,
  "created_at": "...", "updated_at": "..." }
```

## 1.2 تقارير الموظف — `employee-reports`

| العملية | المسار | مَن |
|---|---|---|
| إنشاء تقرير | `POST /api/employee-reports` | **الغسّال + الميكانيكي** (`create.employee_report`) |
| قائمة | `GET /api/employee-reports` | الغسّال + الميكانيكي + **الورشة** (`show.employee_reports`) |
| تفاصيل | `GET /api/employee-reports/{id}` | نفس أعلاه |

- **الورشة تعرض فقط** (لا تنشئ). القائمة تقبل فلاتر: `?order_id=&employee_id=&status=&per_page=`.

**حقول الإنشاء:** `order_id` (مطلوب)، `problem_description` (مطلوب)،
`affected_parts[]?` (مصفوفة نصوص)، `images[]?` (مصفوفة روابط)، `recommendation?`.

**شكل الرد:** `id, order_id, order?, employee_id, employee?, problem_description, affected_parts, images, recommendation, status, reviewed_at, created_at`.

## 1.3 سجلّات GPS — `gps-logs` (⚠️ دفع فقط للموظف)

| العملية | المسار | مَن |
|---|---|---|
| **تسجيل نقطة** | `POST /api/gps-logs` | **الغسّال + الميكانيكي** (`manage.gps_logs`) |
| قائمة / تفاصيل | `GET /api/gps-logs`, `/{id}` | ⛔ **ليست للموظف** — تحتاج `show.gps_logs` ولا يملكها |

- ⚠️ **مفارقة مهمة:** الموظف **يدفع** نقاط GPS (POST) لكن **لا يستطيع قراءتها** (GET يرجع 403).
  فاستعملها للإرسال أثناء تنفيذ الطلب فقط، ولا تبنِ عليها شاشة عرض.

**حقول الإنشاء:** `latitude` (مطلوب -90..90)، `longitude` (مطلوب -180..180)، `order_id?`، `recorded_at?`.

---

# 2) 💳 حالات الدفع — التوثيق الكامل (تحقّق مطلوب)

نعم، وُثّقت طريقة/حالة الدفع سابقاً، وإليك **المجموعة الكاملة** لتفادي أي التباس:

- **`method`** (طريقة): `cash` · `card` · `wallet` · `point` · `package`
- **`status`** (حالة): `pending` · `paid` · `failed` · `refunded`
- **`type`** (نوع): `order` · `package` · `wallet_topup`

**ما يخصّ الموظف/الورشة عملياً:** فقط الدفع النقدي — يبدأ `pending` ثم يؤكّده الموظف ميدانياً فيصبح `paid`:
- `POST /api/payments/{paymentId}/confirm-cash` — لا body، شرطه: `method=cash` و`status=pending`.
- ⚠️ المعرّف = **`payment.id`** (من `order.payments[].id`)، وليس معرّف الطلب.
- الحالات `failed`/`refunded` لا يصنعها الموظف (بوابة الدفع/الأدمن) — لكن قد تظهر له كعرض.

**شكل PaymentResource:** `id, payment_number, type, method, status, amount, points_used`.

---

# 3) 🔄 ترتيب حالات الطلب مقابل الدفع (إجابة السؤال)

هذه أهم نقطة، والإجابة: **هناك مساران منفصلان لا ترتيب إجباري بينهما.**

## مسار حالة الطلب — **مرتّب وإجباري**
```
assigned → in_progress → completed
```
- `start` يتطلب أن تكون الحالة **`assigned`** بالضبط (وإلا خطأ).
- `complete` يتطلب أن تكون **`in_progress`** بالضبط (وإلا خطأ).
- لا يمكن القفز أو الرجوع — كل انتقال محروس (`InvalidBookingStatusTransitionException`).

## مسار الدفع — **مستقلّ، بلا ترتيب مع الحالة**
- `confirm-cash` يتطلب فقط دفعة `cash` حالتها `pending` — **لا يشترط أن يكون الطلب `completed`**.
- أي: يمكن تأكيد النقد والطلب لا يزال `in_progress`، أو بعد اكتماله — **لا فرق، ولا تسلسل مفروض**.

## الخلاصة للواجهة
| السؤال | الجواب |
|---|---|
| هل `assigned → in_progress → completed` مرتّبة؟ | ✅ نعم، إجبارية ومحروسة |
| هل يجب أن يكتمل الطلب قبل تأكيد الدفع؟ | ❌ لا — الدفع مستقلّ |
| هل يمكن تأكيد النقد أثناء `in_progress`؟ | ✅ نعم |
| ما الذي يحكم ظهور زر "تأكيد النقد"؟ | وجود دفعة `method=cash` و`status=pending` — **لا حالة الطلب** |

> توصية UX: اعرض أزرار "دورة الحياة" (start/complete) و"تأكيد النقد" **بشكل مستقل**؛
> لا تعطّل تأكيد النقد بانتظار الإكمال، ولا العكس. كلٌّ يعتمد على شرطه الخاص فقط.

---

# 4) خلاصة المستجدّات (ما يحدّثه الفرونت)
1. **قطع الغيار:** شاشة للميكانيكي (إنشاء + قائمة). الاعتماد على لوحة العميل لا عندك. القطعة من `material_id`.
2. **تقارير الموظف:** إنشاء (غسّال+ميكانيكي) + عرض (يشمل الورشة).
3. **GPS:** إرسال نقاط فقط (لا شاشة عرض للموظف).
4. **حالات الدفع** موثّقة كاملة؛ الموظف يعنيه `cash: pending→paid` فقط.
5. **لا ترتيب** بين إكمال الطلب وتأكيد الدفع — زرّان مستقلّان.
6. **العقود (Contracts) حُذفت** من الباك — أزِل أي أثر لها.
